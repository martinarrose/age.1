let age = Number(prompt("inserisci la prima età:"));
let age2 = Number(prompt("Inserisci la seconda età"));

if (age < age2) {
  alert(age + " e' minore di " + age2);
} else if (age > age2) alert(age + " e' maggiore di " + age2);
